version: "3.9"

services:
  backend:
    build:
      context: ./backend

    container_name: assignment_backend

    ports:
      - "5001:5001"

    env_file:
      - ./backend/.env

    restart: unless-stopped